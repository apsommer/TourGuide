<resources>

    <!-- base app -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="actionBarStyle">@style/AppBarStyle</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

        <!-- app bar -->
        <style name="AppBarStyle" parent="Widget.AppCompat.Light.ActionBar.Solid.Inverse">
            <item name="elevation">0dp</item> <!-- Remove shadow below the app bar -->
        </style>

        <!-- viewpager sliding tabs -->
        <style name="TabStyle" parent="Widget.Design.TabLayout">
            <item name="tabIndicatorColor">#000000</item>
            <item name="tabTextAppearance">@style/TabTextAppearance</item>
        </style>

            <!-- sliding tabs text appearance -->
            <style name="TabTextAppearance" parent="TextAppearance.Design.Tab">

            </style>

</resources>
